<h1>BOOKS</h1>
<form action="/books/search/" method="GET">
  <div>
    Search <input type="text" name="bookSearch">
    <input type="submit" value="Go!">
  </div>
</form>
{{#if books.length}}
  {{#each books}}
    <div class="item">
      <a href="/books/edit/{{id}}">{{title}}</a>
    </div>
  {{/each}}
  <button>Create book</button>
{{else}}
  {{#if book.length}}
    {{#each book}}
      <div class="item">
        <a href="/books/edit/{{id}}">{{title}}</a>
      </div>
    {{/each}}
    <button>Create book</button>
  {{else}}
    <div>
      <p>There is no books atm..</p>
      <button>Create book</button>
    </div>
  {{/if}}
{{/if}}