{{#if book}}
  <div><h1>{{book.title}}</h1></div>
  <div>{{book.author}}</div>
  <div>{{book.abstract}}</div>
  <div>{{book.isbn}}</div>
  <button onclick="window.location.href='/page-editbooks'">Edit</button>
{{else}}
  <p>Book does not exist yet!</p>
{{/if}}